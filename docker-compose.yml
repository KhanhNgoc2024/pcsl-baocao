version: "3.8"
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: pcsl_app
      POSTGRES_PASSWORD: pcsl_dev_pw
      POSTGRES_DB: pcsl_baocao
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
